# Style ต้นแบบ — สกัดจาก prinmath.pdf (หลักคณิตศาสตร์, ม.นเรศวร)

> เอาเฉพาะ **รูปแบบการนำเสนอ** เท่านั้น ห้ามนำเนื้อห

## องค์ประกอบหน้ากระดาษ
- **ระยะบรรทัดกระชับ (single spacing)** — จุดต่างที่ชัดสุดจากเล่มเรา (เราตั้ง `\linespread{2.25}` หลวมมาก)
- Running header: ซ้าย = ชื่อเอกสาร/รหัสวิชา, ขวา = "หน้าที่ X/N"
- Running footer: ซ้าย = ภาคการศึกษา, ขวา = คณะ/มหาวิทยาลัย
- ฟอนต์ serif ไทย, ย่อหน้าปกติ

## นิยาม (บทนิยาม)
- **กล่องมีแถบหัวสีเข้ม** ตัวหนังสือขาว: `บทนิยาม X.Y: <ชื่อหัวข้อ>` เลข chapter.number + ชื่อ
- เนื้อในกล่อง = **นิยามล้วน**: ขึ้นต้น "ให้ A และ B เป็นเซต" แล้ว bullet ศัพท์ที่นิยาม แต่ละตัวมี **สมการ display** (set-builder)
- ไม่มีสมบัติ/กฎในกล่อง
- **1 ประโยคเกริ่นก่อนกล่อง** + **คำอธิบาย/สัญชาตญาณเป็น prose หลังกล่อง**
  → ตรงกับที่เราเพิ่งปรับ ch2 ทุกประการ

## ทฤษฎีบท
- **ไม่ใส่กล่อง** — หัวตัวหนา `ทฤษฎีบท X.Y.` + ชื่อ (เอียง) + `:` แล้วตามด้วย **ลิสต์เลข 1. 2. 3.** ของสมบัติ/กฎ
  → สมบัติ/กฎอยู่ใน "ทฤษฎีบท" (ลิสต์) ไม่ใช่ในกล่องนิยาม (ตรงกับที่เราย้ายกฎ union/intersection ออกมา)

## ตัวอย่าง
- `ตัวอย่าง N.` ตัวหนา — **เลขรันต่อเนื่องทั้งเล่ม** (เห็น "ตัวอย่าง 53") ไม่ใช่ per-chapter
- โจทย์ย่อยใช้อักษรไทย ก. ข. ค. ง.

## วิธีทำ / การพิสูจน์
- หัว **การพิสูจน์** / **วิธีทำ** ตัวหนา
- **ขั้นตอนเป็นสมการเรียงบรรทัด** จัดชิดตัวดำเนินการ เช่น chain ของ `⇒`:
  ```
  x∈A ⇒ {x}⊆A
       ⇒ {x}∈P(A)
       ⇒ {x}∈P(B)
       ⇒ x∈B
  ```
  → ตรงกับที่เราเพิ่งทำ ch4 (align math-steps)

## ศัพท์
- **ศัพท์ไทยตัวหนา (English gloss)** — ตรงกับ house style เรา
- Set-builder ใช้ `:` (เขา) — **เราคง `|` ตาม house style เดิม** (ไม่ก๊อป)

---

## สรุป: เล่มเรา match อยู่แล้วเยอะ
สิ่งที่ทำไปแล้วสอดคล้อง: เกริ่นก่อนนิยาม, กล่องนิยามล้วน, ย้ายสมบัติออกเป็นลิสต์/ทฤษฎีบท, วิธีทำเป็น align

## ต่างที่ต้องตัดสินใจปรับ (ระดับ class/ทั้งเล่ม)
1. ✅ **ADOPTED (2026-07-24)** ระยะบรรทัด 2.25 → **`\linespread{1.5}`** (แก้ `kkubee_edit_all.cls` บรรทัด 38,40) — ทั้งเล่ม 325→270 หน้า
2. ✅ **ADOPTED** **กล่องนิยามแถบหัวเข้ม "บทนิยาม X.Y: ชื่อ"** — เขียน `\newenvironment{definition}` เป็น tcolorbox ใน `mathstyle.sty` (colbacktitle=defTitle เขียวเข้ม ตัวอักษรขาว), ถอด `\newtheorem{definition}` ใน `main.tex`. ใช้ตัวนับ `thrm` ร่วม (`\refstepcounter{thrm}`) เลขต่อเนื่องเหมือนเดิม, รองรับ `\begin{definition}[ชื่อ]`
3. ❌ ไม่เอา — ทฤษฎีบทคงกล่องฟ้าไว้
4. ❌ ไม่เอา — เลขตัวอย่างคง per-chapter "ตัวอย่างที่ 2.5"

## TODO ต่อยอด (ถ้าต้องการให้ตรง prinmath ยิ่งขึ้น)
- นิยามส่วนใหญ่ยังไม่มี `[ชื่อ]` → โชว์แค่ "บทนิยาม X.Y". prinmath มีชื่อหัวข้อทุกอัน — เติม `[ชื่อ]` ได้ภายหลัง (งานเนื้อหา)
