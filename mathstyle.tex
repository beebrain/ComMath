%% ============================================================
%% mathstyle.tex — Modern International Textbook Style
%% Style 1: Left Accent Bar (Springer / MIT Press / Cambridge)
%% ============================================================
%% วิธีใช้: \input{mathstyle} ใน preamble ของ main.tex
%%          หลังจาก \usepackage[most]{tcolorbox}
%%          และหลังจาก \newtheorem definitions
%% ============================================================

%% ─────────────── Color Palette ───────────────
%% แต่ละ environment มีสีเฉพาะเพื่อสร้าง visual hierarchy

%% ทฤษฎีบท (Theorem) — Navy Blue
\definecolor{thrmBar}{HTML}{1B4F72}
\definecolor{thrmBg}{HTML}{EBF5FB}
\definecolor{thrmTitle}{HTML}{154360}

%% นิยาม (Definition) — Emerald Green
\definecolor{defBar}{HTML}{0E6655}
\definecolor{defBg}{HTML}{E8F8F5}
\definecolor{defTitle}{HTML}{0B5345}

%% ตัวอย่าง (Example) — Warm Orange
\definecolor{exBar}{HTML}{E67E22}
\definecolor{exBg}{HTML}{FEF5E7}
\definecolor{exTitle}{HTML}{CA6F1E}

%% บทแทรก (Lemma / Proposition) — Purple
\definecolor{propBar}{HTML}{6C3483}
\definecolor{propBg}{HTML}{F4ECF7}
\definecolor{propTitle}{HTML}{5B2C6F}

%% หมายเหตุ (Remark) — Cool Gray
\definecolor{remBar}{HTML}{566573}
\definecolor{remBg}{HTML}{F2F3F4}
\definecolor{remTitle}{HTML}{2C3E50}

%% บทสรุป (Corollary) — Teal
\definecolor{corBar}{HTML}{148F77}
\definecolor{corBg}{HTML}{E8F6F3}
\definecolor{corTitle}{HTML}{117A65}

%% แบบฝึกหัด (Exercise) — Brown
\definecolor{exerBar}{HTML}{7B4F2A}
\definecolor{exerBg}{HTML}{FDF2E9}
\definecolor{exerTitle}{HTML}{6E3B12}

%% คำเตือน (Warning) — Red
\definecolor{warnBar}{HTML}{C0392B}
\definecolor{warnBg}{HTML}{FDEDEC}
\definecolor{warnTitle}{HTML}{A93226}

%% พิสูจน์ (Proof) — Dark Gray (subtle)
\definecolor{proofBar}{HTML}{7F8C8D}
\definecolor{proofBg}{HTML}{FAFAFA}


%% ─────────────── Base Style Template ───────────────
%% ใช้ tcolorboxenvironment เพื่อครอบ amsthm environments ที่มีอยู่แล้ว
%% ลักษณะ: เส้น 3pt ด้านซ้าย, background สีอ่อน, มุมโค้งเล็กน้อย

%% ── ทฤษฎีบท (thrm) ──────────────────────────────
\tcolorboxenvironment{thrm}{
  enhanced jigsaw,
  breakable,
  colback=thrmBg,
  colframe=thrmBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{thrmTitle},
}

%% ── นิยาม (definition) ──────────────────────────
\tcolorboxenvironment{definition}{
  enhanced jigsaw,
  breakable,
  colback=defBg,
  colframe=defBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{defTitle},
}

%% ── ตัวอย่าง (ex) ───────────────────────────────
\tcolorboxenvironment{ex}{
  enhanced jigsaw,
  breakable,
  colback=exBg,
  colframe=exBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{exTitle},
}

%% ── บทแทรก (prop) ───────────────────────────────
\tcolorboxenvironment{prop}{
  enhanced jigsaw,
  breakable,
  colback=propBg,
  colframe=propBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{propTitle},
}

%% ── หมายเหตุ (rem) ──────────────────────────────
\tcolorboxenvironment{rem}{
  enhanced jigsaw,
  breakable,
  colback=remBg,
  colframe=remBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{remTitle},
}

%% ── บทสรุป (cor) ────────────────────────────────
\tcolorboxenvironment{cor}{
  enhanced jigsaw,
  breakable,
  colback=corBg,
  colframe=corBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{corTitle},
}

%% ── คำเตือน (warning) ───────────────────────────
\tcolorboxenvironment{warning}{
  enhanced jigsaw,
  breakable,
  colback=warnBg,
  colframe=warnBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{warnTitle},
}

%% ── แบบฝึกหัด (exercise) ────────────────────────
\tcolorboxenvironment{exercise}{
  enhanced jigsaw,
  breakable,
  colback=exerBg,
  colframe=exerBar,
  boxrule=0pt,
  leftrule=3pt,
  arc=2pt,
  outer arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=12pt,
  after skip=12pt,
  fonttitle=\bfseries\color{exerTitle},
}


%% ─────────────── Proof Environment ───────────────
%% สไตล์พิสูจน์แบบ minimal — เส้นซ้ายสีเทาอ่อน
%% ใช้ร่วมกับ \begin{proof}...\end{proof} จาก amsthm
\tcolorboxenvironment{proof}{
  enhanced jigsaw,
  breakable,
  colback=proofBg,
  colframe=proofBar,
  boxrule=0pt,
  leftrule=2pt,
  arc=0pt,
  outer arc=0pt,
  left=10pt, right=10pt, top=6pt, bottom=6pt,
  before skip=8pt,
  after skip=8pt,
}


%% ─────────────── QED Symbol ───────────────
%% เปลี่ยน QED symbol เป็นสี่เหลี่ยมเต็ม (filled square)
\renewcommand{\qedsymbol}{$\blacksquare$}


%% ─────────────── Solution Environment ───────────────
%% environment สำหรับเฉลยตัวอย่าง (optional)
\newenvironment{solution}{%
  \par\noindent\textbf{\color{exTitle}วิธีทำ.}\quad\ignorespaces
}{%
  \hfill$\diamond$\par\medskip
}


%% ============================================================
%% สรุป Environments ที่ใช้งานได้:
%% ────────────────────────────────────────────────
%%   \begin{thrm}...\end{thrm}           ทฤษฎีบท (น้ำเงินเข้ม)
%%   \begin{definition}...\end{definition} นิยาม (เขียวมรกต)
%%   \begin{ex}...\end{ex}               ตัวอย่าง (ส้ม)
%%   \begin{prop}...\end{prop}            บทแทรก (ม่วง)
%%   \begin{rem}...\end{rem}             หมายเหตุ (เทา)
%%   \begin{cor}...\end{cor}             บทสรุป (เขียวน้ำทะเล)
%%   \begin{warning}...\end{warning}     คำเตือน (แดง)
%%   \begin{exercise}...\end{exercise}   แบบฝึกหัด (น้ำตาล)
%%   \begin{proof}...\end{proof}         พิสูจน์ (เทาอ่อน)
%%   \begin{solution}...\end{solution}   วิธีทำ (ส้ม, ไม่มีกรอบ)
%% ============================================================
