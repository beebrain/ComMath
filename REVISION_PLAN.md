# แผนปรับปรุงคุณภาพเนื้อหา ComMath (รอบ 2026-08-08)

เอกสารนี้เป็นสัญญาการทำงานของทุก agent ในรอบนี้ ห้ามแก้ไฟล์ `.tex` ใด ๆ
ก่อนอ่านหัวข้อ "กฎ 12 ข้อ" จบ

## เป้าหมาย

ยกคุณภาพเนื้อหาของ `chapters/chapter01.tex` ถึง `chapter06.tex` ให้ผู้เรียน
ระดับปริญญาตรีอ่านเองแล้วเข้าใจได้โดยไม่ต้องมีผู้สอนอธิบายเพิ่ม

**ขอบเขตที่ทำ** ขยายคำอธิบายที่สั้นหรือกระโดด เพิ่มตัวอย่างเรียงจากง่ายไปยาก
เพิ่มการอ้างอิงรูปและตารางในเนื้อความ จัดตำแหน่งรูปให้ใกล้จุดที่กล่าวถึง
ทำศัพท์ให้ตรงกันทั้งเล่ม เกลาย่อหน้าที่แตกถี่เกินไป

**ขอบเขตที่ห้ามแตะ** ห้ามเพิ่มหัวข้อใหม่ ห้ามตัดหัวข้อเดิม ห้ามเปลี่ยนลำดับ
หัวข้อ ห้ามแตะไฟล์ `chapter0*_or.tex` (หน้า มคอ./แผนการสอน)

ไม่มีการจำกัดจำนวนหน้า เล่มปัจจุบัน 253 หน้า

## กฎ 12 ข้อ (ห้ามทิ้งข้อใดข้อหนึ่ง)

1. **ห้ามเพิ่มหรือตัดหัวข้อ** โครงสร้าง `\section` และ `\subsection` นิ่งแล้ว
   งานคือเติมเนื้อในหัวข้อที่มีอยู่เท่านั้น
2. **ทุกหัวข้อต้องมีร้อยแก้วเกริ่นนำก่อนขึ้นกล่อง ตัวอย่าง หรือหัวข้อย่อย**
   อย่างน้อย 3 บรรทัด บอกว่าหัวข้อนี้แก้ปัญหาอะไรและต่อจากหัวข้อก่อนหน้าอย่างไร
   ห้ามขึ้นหัวข้อแล้วเข้ากล่องนิยามทันที
3. **ตัวอย่างต้องเรียงจากง่ายไปยาก** ตัวอย่างแรกของทุกหัวข้อต้องเป็นกรณีตรงไปตรงมา
   ที่แทนค่าแล้วได้คำตอบทันที แล้วจึงไต่ระดับไปกรณีที่ต้องใช้หลายกฎหรือหลายขั้น
   หัวข้อที่มีแต่ตัวอย่างยากต้องเติมตัวอย่างง่ายนำหน้า
4. **ทุกรูปและทุกตารางต้องถูกอ้างในเนื้อความ** ด้วย `\ref` หรือ `\autoref`
   และประโยคอ้างอิงต้องบอกว่าให้ดูอะไรในรูปนั้น ห้ามเขียนลอย ๆ ว่า
   "ตารางที่ X แสดง..." เป็นบรรทัดโดด ให้ผนวกเข้ากับร้อยแก้ว เช่น
   "...ดังแสดงในตารางที่ \ref{...}" หรือ "พิจารณาผลลัพธ์ตามตารางที่ \ref{...}"
   คำบรรยายใต้รูป (caption) ต้องตรงกับสิ่งที่ประโยคอ้างอิงบอกไว้
5. **ห้ามย่อหน้าถี่เกินจำเป็น** ประเด็นที่ไม่ใช่ประเด็นใหญ่ให้เขียนต่อในย่อหน้าเดิม
   ห้ามใช้ `\subsection` หรือ `\subsubsection` กับประเด็นเล็กที่เขียนเป็นย่อหน้าได้
6. **ศัพท์ต้องตรงกันทั้งเล่ม** ตรวจด้วย `python3 tools/term_audit.py` ก่อน commit
   ทุกครั้ง คำตัดสินที่ลงแล้วบันทึกใน `glossary.tsv` เล่มนี้ไม่มีหน้าอภิธานศัพท์
   จึงต้องอาศัยความสม่ำเสมอในเนื้อหาแทน
7. **ศัพท์เขียนไทยตัวหนานำ แล้ววงเล็บอังกฤษ เฉพาะครั้งแรกที่ปรากฏในบท**
   ครั้งถัดไปใช้ภาษาไทยล้วน ห้ามบัญญัติศัพท์ใหม่เอง ถ้าไม่แน่ใจให้ตรวจกับ
   ราชบัณฑิตด้วย skill `orst-lookup` ก่อน
8. **วิธีทำต้องเป็นขั้นตอนคณิตศาสตร์** ใช้ `align*` จัดแนวที่ `=` `\equiv` หรือ
   `\Leftrightarrow` ระบุชื่อกฎท้ายบรรทัดทุกขั้น จบด้วย `\therefore` และ `\solhere`
   ห้ามใช้ `solsteps` และ `\mathoval` ซึ่งเลิกใช้แล้วทั้งเล่ม
9. **ห้ามใช้เครื่องหมาย `:` ในข้อความภาษาไทย** ห้าม emoji ห้าม markdown `**`
   ในไฟล์ `.tex` ห้าม `---` แทนการขยายความ ให้ใช้คำเชื่อมไทยแทน
10. **ห้ามสำนวน AI และสำนวนนวนิยาย** ห้ามคำว่า "มโนภาพ" "มีบทบาทสำคัญอย่างยิ่ง"
    "เป็นเครื่องยืนยัน" "สะท้อนให้เห็นถึง" ห้ามบทสรุปสูตรสำเร็จ ห้ามคำขยายลอย ๆ
    ที่ไม่มีหลักฐานรองรับ เช่น "ได้อย่างมีประสิทธิภาพ" "ดีที่สุด"
11. **ห้ามอ้างอิงปลอม** ทุก `\cite` ต้องมี entry จริงใน `references.bib` หรือ
    `author_pubs.bib` ห้ามแต่ง DOI หรือชื่อผลงานขึ้นเอง ถ้าต้องการอ้างงานใหม่
    ให้ค้นข้อมูลจริงก่อนแล้วเพิ่ม entry
12. **ทุกบทต้อง build ผ่านก่อน commit** `latexmk -xelatex -interaction=nonstopmode main.tex`
    ต้อง EXIT 0 และไม่มี undefined reference ไม่มี multiply defined label
    ไม่มี missing character

## ข้อกำหนดการอธิบายให้เข้าใจง่าย

ยึดตาม skill `easy-explanation-guide` โดยปรับให้เข้ากับเล่มนี้

**ลำดับการอธิบายในทุกหัวข้อ** เริ่มจากปัญหาหรือสถานการณ์รูปธรรมที่ผู้เรียนนึกออก
ต่อด้วยแนวคิดเชิงสัญชาตญาณว่าเครื่องมือนี้ทำอะไร แล้วจึงเข้าสัญลักษณ์และนิยาม
ปิดท้ายด้วยการบอกว่าผลลัพธ์นำไปใช้ต่ออย่างไร

**เมื่อแสดงสมการ ต้องอธิบายบทบาทของสัญลักษณ์ทุกตัว** ห้ามปล่อยให้ผู้อ่านเดาว่า
ตัวแปรตัวไหนหมายถึงอะไร

**เมื่อพูดถึงมิติสูงหรือกรณีทั่วไป ให้เริ่มจากกรณีเล็กที่สุดก่อนเสมอ** เช่น
อธิบายเมทริกซ์ $2\times2$ ให้จบก่อนขยายไป $n\times n$ อธิบายตัวแปร 2 ตัว
ก่อนขยายไป $n$ ตัว

**ข้อควรระวังที่ปรับจาก skill ต้นทาง** skill `easy-explanation-guide` เขียนไว้
สำหรับตำรา Neural Network และอนุญาตคำว่า "พิจารณามโนภาพ" แต่เล่มนี้ตัดคำว่า
"มโนภาพ" ออกไปแล้ว 17 จุด ห้ามนำกลับมา ให้ใช้ "พิจารณา" "ให้สังเกตว่า"
หรือ "ในทางปฏิบัติ" แทน และเล่มนี้ไม่มี `glossary.tsv` แบบเล่ม NN
ให้ยึด `tools/term_audit.py` แทน

## กระบวนการต่อหนึ่งบท

ทำทีละบท ห้ามทำหลายบทพร้อมกัน เพราะศัพท์และสไตล์จะแตกกันและ rollback ยาก

1. **commit จุดกู้คืน** ก่อนแตะไฟล์ทุกครั้ง
2. **วิจารณ์** ส่งบทให้ agent `academic-book-critic` อ่านทั้งบท รายงานจุดที่อธิบายสั้น
   ตัวอย่างไม่เรียงระดับ รูปหรือตารางไม่ถูกอ้าง ย่อหน้าถี่เกิน และคณิตศาสตร์ผิด
   พร้อมเลขบรรทัด
3. **ตัดสินและเขียน** ผู้ควบคุมกลั่นรายงาน ตัดข้อที่ขัดกฎ 12 ข้อทิ้ง แล้วแก้ไฟล์
   หรือมอบหมายให้ agent `academic-writer-th` เขียนส่วนที่ต้องขยายความ
4. **ตรวจกลับ** รัน `python3 tools/term_audit.py` และ build ต้องเขียว
   แล้วส่งบทให้ critic อ่านซ้ำ ถ้ายังมีข้อหนักเหลือ กลับไปข้อ 3
5. **commit ผลงาน** พร้อมสรุปว่าแก้อะไรบ้าง แล้วส่งให้ผู้เขียนตรวจ

## ทีม agent

| บทบาท | agent | หน้าที่ |
|---|---|---|
| ผู้ควบคุม | เซสชันหลัก | ตัดสินใจทุกอย่าง แก้ไฟล์จริง commit และคุมไม่ให้ผิดกฎ 12 ข้อ |
| ผู้วิจารณ์ | `academic-book-critic` | อ่านทั้งบท ชี้จุดอ่อนพร้อมเลขบรรทัด ห้ามแก้ไฟล์ |
| ผู้เขียน | `academic-writer-th` | ร่างเนื้อหาขยายความและตัวอย่างใหม่ตามที่ผู้ควบคุมสั่ง |
| ผู้ตรวจโครง | `math-textbook-architect` | ตรวจลำดับการเรียนรู้ในบท ใช้เมื่อลำดับเนื้อหาในบทมีปัญหา |
| ผู้ค้นข้อมูล | `Explore` หรือ `WebSearch` | หาข้อมูลอ้างอิงจริงเมื่อต้องเพิ่ม `\cite` |

## เครื่องมือ

- ตรวจศัพท์ `python3 tools/term_audit.py` (เพิ่ม `--all` เพื่อดูทุกคู่)
- ตรวจศัพท์กับราชบัณฑิต skill `orst-lookup`
- build `latexmk -xelatex -interaction=nonstopmode main.tex`
- ดูผลจริง `pdftoppm -f <หน้า> -l <หน้า> -r 120 -png main.pdf out`

## ลำดับการทำ

ทำ **บทที่ 1 เป็นบทนำร่อง** แล้วหยุดให้ผู้เขียนตรวจก่อน ถ้าผ่านจึงใช้ผลของ ch1
เป็นต้นแบบทำบทที่เหลือตามลำดับ ch4 (บางที่สุด) ch3 ch2 ch5 ch6

## สถานะรอบนี้

- [x] commit จุดกู้คืน `7aed8a7`
- [x] แก้ skill `.agent/commath-write-example/SKILL.md` ที่ยังสอน `solsteps` และ
      `\mathoval` ซึ่งเลิกใช้แล้ว (ถ้าไม่แก้ agent จะเขียนของเก่ากลับเข้ามา)
- [x] สร้าง `tools/term_audit.py` ได้ baseline ศัพท์อังกฤษ 301 คำ ขัดกัน 52 คำ
- [ ] ch1 นำร่อง
- [ ] ch4 ch3 ch2 ch5 ch6

## ศัพท์ที่ตัดสินแล้วในรอบนี้

ตรวจกับราชบัณฑิตยสภาผ่าน skill `orst-lookup` สาขาคณิตศาสตร์และคอมพิวเตอร์

| อังกฤษ | เดิมในเล่ม | ที่จะใช้ | เหตุผล |
|---|---|---|---|
| associative | กฎเปลี่ยนกลุ่ม (ch1) / การเปลี่ยนหมู่ (ch5) | **กฎการเปลี่ยนหมู่** | ราชบัณฑิตให้ "เปลี่ยนหมู่" ch5 ใช้อยู่แล้ว |
| absorption | กฎดูดกลืน (ch1) / กฎการดูดกลืน (ch6) | **กฎการดูดกลืน** | ราชบัณฑิตให้ "การดูดกลืน" ch6 ใช้อยู่แล้ว |
| distributive | กฎแจกแจง / กฎการแจกแจง | **กฎการแจกแจง** | ราชบัณฑิตไม่มีข้อมูล เลือกให้ตรงรูปกับสองข้อบน |
| canonical form | รูปแบบปกติ / รูปแบบบัญญัติ | **รูปแบบบัญญัติ** | ราชบัณฑิตให้ "รูปแบบบัญญัติ" |
| cartesian product | ผลคูณคาร์ทีเซียน / คาร์ทีเซียนพรอแดกต์ | **ผลคูณคาร์ทีเซียน** | ราชบัณฑิตยืนยัน ทับศัพท์ตัดทิ้ง |
| commutative | กฎสลับที่ | **กฎการสลับที่** | ราชบัณฑิตให้ "สลับที่" ปรับรูปให้ตรงกับข้ออื่น |
| predicate | ภาคแสดง | **ภาคแสดง** (คงไว้) | ราชบัณฑิตสาขาคอมพิวเตอร์ให้ "เพรดิเคต" แต่ตำราตรรกศาสตร์ไทยใช้ "ภาคแสดง" ทั่วไป |
| complement | คอมพลีเมนต์ / ส่วนเติมเต็ม | **รอผู้เขียนตัดสิน** | ราชบัณฑิตให้ "ส่วนเติมเต็ม" แต่เล่มตั้ง "คอมพลีเมนต์" เป็นมาตรฐานไว้ ปนกันอยู่ใน ch2 |
